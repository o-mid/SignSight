# SignSight

Portfolio demo wallet. See the request before you sign.

React Native CLI app (TypeScript). A dApp pairs over WalletConnect; this wallet shows a human summary of inbound `eth_sendTransaction` and `personal_sign` requests.

Not a custodial product. No mainnet funds. No seed phrase flow.

Setup notes land in later commits.
